# MerchantPay v0.9.6.2 — build-error fixes

- Prisma Client is regenerated automatically on install, dev startup and production build.
- Prisma packages are pinned to 6.19.3, matching the lockfile and current generated-client expectations.
- Removed the invalid `holdUntil` read from SubscriptionPayment in the admin Money Center; `holdUntil` belongs to MerchantPayout only.
- Kept the Paytota schema fields (`mobileMoneyNetwork`, `providerTransactionId`, `holdUntil` on payouts) aligned with `prisma/schema.prisma`.
- Deployment remains Vercel Hobby-compatible; no frequent Vercel Cron was added.
