"use client";import {useState} from "react";import Link from "next/link";type R={id:string;name:string;email:string;plan:string;status:string;expires:string;renewed:string};export default function AdminSearch({rows}:{rows:R[]}){const[q,setQ]=useState("");const x=rows.filter(r=>(r.name+r.email+r.plan+r.status).toLowerCase().includes(q.toLowerCase()));return <section className="panel"><div className="section-heading"><div><h2>All merchants</h2><p className="muted">Search and open any merchant profile.</p></div><input aria-label="Search merchants" placeholder="Search business, email, plan…" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Business</th><th>Owner email</th><th>Plan</th><th>Status</th><th>Expires</th><th>Renewed</th></tr></thead><tbody>{x.map(r=><tr key={r.id}><td><Link href={`/admin/businesses/${r.id}`}><strong>{r.name}</strong></Link></td><td>{r.email}</td><td>{r.plan}</td><td>{r.status}</td><td>{r.expires}</td><td>{r.renewed}</td></tr>)}</tbody></table></div></section>}
