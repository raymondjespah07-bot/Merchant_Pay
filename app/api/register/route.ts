import {NextResponse} from "next/server";import {db} from "@/lib/db";import {hashPassword,token} from "@/lib/auth";
export async function POST(req:Request){try{const b=await req.json();const name=String(b.name??"").trim(),email=String(b.email??"").trim().toLowerCase(),password=String(b.password??"");if(!name||!email||password.length<8)return NextResponse.json({error:"Name, email and an 8+ character password are required."},{status:400});if(await db.user.findUnique({where:{email}}))return NextResponse.json({error:"An account with that email already exists."},{status:409});const u=await db.user.create({data:{name,email,passwordHash:hashPassword(password)}});const r=NextResponse.json({ok:true});r.cookies.set("merchantpay_session",`${u.id}.${token()}`,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:2592000});return r}catch{return NextResponse.json({error:"Unable to create account."},{status:500})}}
