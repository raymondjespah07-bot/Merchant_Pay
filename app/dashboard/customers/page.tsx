import Link from "next/link"; import {redirect} from "next/navigation"; import {db} from "@/lib/db"; import {currentBusiness} from "@/lib/current-user"; import CustomerClient from "./customer-client";
export default async function Customers(){const x=await currentBusiness();if(!x)redirect("/login");if(x.business.suspended)redirect("/dashboard/suspended");const customers=await db.customer.findMany({where:{businessId:x.business.id},orderBy:{createdAt:"desc"}});return <main className="dash"><aside className="side"><strong>MerchantPay</strong><nav className="nav"><Link href="/dashboard">Dashboard</Link><Link href="/dashboard/invoices">Invoices</Link><Link href="/dashboard/payments">Payments</Link><Link className="active" href="/dashboard/customers">Customers</Link><Link href="/dashboard/items">Products &amp; Services</Link><Link href="/dashboard/reports">Reports</Link><Link href="/dashboard/settings">Settings</Link></nav></aside><section className="main"><div className="top"><div><p className="muted">{x.business.name}</p><h1>Customers</h1><p className="muted">Keep customer records ready for invoices and payment collection.</p></div><form action="/api/logout" method="post"><button className="btn">Sign out</button></form></div><CustomerClient initialCustomers={customers.map(c=>({...c,createdAt:c.createdAt.toISOString()}))}/></section></main>}
