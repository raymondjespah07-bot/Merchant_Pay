"use client"; import Link from "next/link"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Login(){const r=useRouter();const[e,setE]=useState("");const[b,setB]=useState(false);async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));setE("");setB(true);try{const q=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const d=await q.json();if(!q.ok){setE(d.error??"Login failed.");return}r.push(d.isAdmin?"/admin":"/dashboard")}catch{setE("Could not connect to MerchantPay. Is the development server running?")}finally{setB(false)}}return <main className="auth"><section className="card"><div className="eyebrow">MerchantPay</div><h1>Welcome back</h1><form className="form" onSubmit={go}><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label>{e&&<div className="error">{e}</div>}<button className="btn primary" disabled={b}>{b?"Logging in...":"Log in"}</button></form><p className="foot"><Link href="/register">Create an account</Link></p></section></main>}
