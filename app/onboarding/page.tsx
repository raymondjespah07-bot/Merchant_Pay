"use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Onboarding(){const r=useRouter();const[e,setE]=useState("");const[b,setB]=useState(false);async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));setE("");setB(true);try{const q=await fetch("/api/onboarding",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const d=await q.json();if(!q.ok){setE(d.error??"Business setup failed.");return}r.push("/dashboard")}catch{setE("Could not connect to MerchantPay. Is the development server running?")}finally{setB(false)}}return <main className="auth"><section className="card"><div className="eyebrow">Step 2 of 2</div><h1>Set up your business</h1><p className="muted">Tell us the basics so your workspace belongs to the right business.</p><form className="form" onSubmit={go}><label>Business name<input name="businessName" required placeholder="Your shop name"/></label><label>Business phone<input name="businessPhone" placeholder="+256 ..."/></label><label>Mobile money phone number<input name="mobileMoneyPhone" type="tel" required placeholder="+256 7XX XXX XXX"/><small className="muted">This mobile phone number will receive the customer invoice payments, ensure it's the right one.</small></label><label>Mobile money network<select name="mobileMoneyNetwork" required><option value="MTN">MTN Mobile Money</option><option value="AIRTEL">Airtel Money</option></select></label><label>Currency<select name="currency" defaultValue="UGX"><option value="UGX">UGX — Ugandan Shilling</option></select></label>{e&&<div className="error">{e}</div>}<button className="btn primary" disabled={b}>{b?"Setting up...":"Create business"}</button></form></section></main>}
