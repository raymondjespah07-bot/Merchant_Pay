"use client"; import Link from "next/link"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Register(){const r=useRouter();const[e,setE]=useState("");const[b,setB]=useState(false);async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));setE("");setB(true);try{const q=await fetch("/api/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const d=await q.json();if(!q.ok){setE(d.error??"Registration failed.");return}r.push("/onboarding")}catch{setE("Could not connect to MerchantPay. Is the development server running?")}finally{setB(false)}}return <main className="auth"><section className="card"><div className="eyebrow">MerchantPay</div><h1>Create your account</h1><p className="muted">Your first step into MerchantPay.</p><form className="form" onSubmit={go}><label>Name<input name="name" required autoComplete="name"/></label><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" minLength={8} required autoComplete="new-password"/></label>{e&&<div className="error">{e}</div>}<button className="btn primary" disabled={b}>{b?"Creating...":"Create account"}</button></form><p className="foot">Already have an account? <Link href="/login">Log in</Link></p></section></main>}
